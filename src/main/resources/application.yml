spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reusai?useSSL=false&serverTimezone=UTC
    username: root
    password: 00D42s@l
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false